import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.violinjournal.app',
  appName: 'Violin Journal',
  webDir: 'dist',
  android: {
    backgroundColor: '#5c3d1e',
  },
};

export default config;